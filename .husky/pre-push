#!/usr/bin/env sh

echo "🚀 Running pre-push checks..."

# Skip all checks in CI environment (GitHub Actions)
if [ "$CI" = "true" ] || [ "$GITHUB_ACTIONS" = "true" ]; then
    echo "ℹ️  Running in CI environment, skipping pre-push checks"
    exit 0
fi

# Get the current branch
current_branch=$(git rev-parse --abbrev-ref HEAD)

# Skip checks for main and dev branches (they should go through PR)
if [ "$current_branch" = "main" ] || [ "$current_branch" = "dev" ]; then
    echo "⚠️  Direct push to $current_branch detected. Please use Pull Requests for main/dev branches."
    echo "   If this is intentional (e.g., hotfix), use --no-verify to bypass this check."
    exit 1
fi

# Run full linting on all modules
echo "🔍 Running linting on all modules..."
make lint || {
    echo "❌ Linting failed. Fix issues before pushing."
    exit 1
}

# Run comprehensive security scan
echo "🔒 Running comprehensive security scan..."
make security-scan || {
    echo "❌ Security scan failed. Fix issues before pushing."
    exit 1
}

# Run tests before push
echo "🧪 Running tests..."
npm test || {
    echo "❌ Tests failed. Fix issues before pushing."
    exit 1
}

echo "✅ Pre-push checks passed!"
