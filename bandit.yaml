# Bandit Configuration - Run all checks but exclude unnecessary directories
exclude_dirs:
  - .venv
  - venv
  - node_modules
  - cdk.out
  - tests
  - __pycache__
  - .git
  - dist
  - build

# Suppress warnings about comments that look like test names
skips: []
